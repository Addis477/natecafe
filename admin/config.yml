backend:
  name: git-gateway
  branch: main

media_library:
  name: cloudinary
  config:
    cloud_name: "dagujducp"           # ← Your cloud name
    api_key: "869678112792591"         # ← Your API key
    # api_secret is NOT needed here - it's handled by the media library
    default_transformations:
      - - fetch_format: auto
          quality: auto
          width: 800
          crop: limit

collections:
  - name: "menu"
    label: "Menu Items"
    label_singular: "Menu Item"
    folder: "content/menu"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "Dish Name", name: "title", widget: "string"}
      - label: "Category"
        name: "category"
        widget: "select"
        options:
          - {label: "Appetizers", value: "Appetizers"}
          - {label: "Main Courses", value: "Main Courses"}
          - {label: "Ethiopian Specials", value: "Ethiopian Specials"}
          - {label: "Coffee & Drinks", value: "Coffee & Drinks"}
          - {label: "Mocktails", value: "Mocktails"}
          - {label: "Desserts", value: "Desserts"}
      - {label: "Description", name: "body", widget: "text", required: false}
      - {label: "Price (ETB)", name: "price", widget: "number"}
      - label: "Dish Photo"
        name: "image"
        widget: "image"
        required: false
        media_library:
          config:
            multiple: false
      - {label: "Featured Item", name: "featured", widget: "boolean", default: false}
      - {label: "Tags", name: "tags", widget: "list", default: [], required: false}
